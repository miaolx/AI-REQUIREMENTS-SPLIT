# Codex Python SDK 默认复用本机已有的 Codex 登录态。
# 如需 API Key 登录，请不要提交真实密钥；由部署环境安全注入。
# OPENAI_API_KEY="sk-..."

# 固定模型与推理强度，避免不同部署或本地默认配置造成结果漂移。
CODEX_MODEL="gpt-5.6-sol"
CODEX_REASONING_EFFORT="high"

# 可选：分析缓存目录。相同输入会直接复用缓存结果。
# ANALYSIS_CACHE_DIR="C:/path/to/cache"

PORT=8000
